import Foundation

extension ProjectUnitsListView {
    public enum Column: CaseIterable {
        case name
        case floor
        case area
        case bedrooms
        case totalPrice
    }
}
