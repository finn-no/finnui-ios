import Foundation

extension ProjectUnitsListView {
    public struct Titles {
        public let title: String
        public let sortingTitle: String

        public init(title: String, sortingTitle: String) {
            self.title = title
            self.sortingTitle = sortingTitle
        }
    }
}
